# Supabase Configuration
# Copy this file to .env and fill in your actual values
# Do not commit the .env file to version control

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
VITE_SUPABASE_PROJECT_ID=your_supabase_project_id

# How to get these values:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Go to Settings > API
# 4. Copy the Project URL as VITE_SUPABASE_URL
# 5. Copy the anon/public key as VITE_SUPABASE_PUBLISHABLE_KEY
# 6. The project ID is in the URL and project settings

# App Lock Configuration (optional)
# To enable app-wide password protection, set these values:
# VITE_APP_LOCK_HASH=your_pbkdf2_hash_here
# VITE_APP_LOCK_SALT=your_random_salt_here
# VITE_APP_LOCK_DISABLED=false

# To disable app lock in development:
# VITE_APP_LOCK_DISABLED=true

# To generate the hash and salt, use the following in your browser console:
# const password = 'your_password_here';
# const salt = crypto.getRandomValues(new Uint8Array(32)).reduce((str, byte) => str + byte.toString(16).padStart(2, '0'), '');
# const encoder = new TextEncoder();
# const keyMaterial = await crypto.subtle.importKey('raw', encoder.encode(password), { name: 'PBKDF2' }, false, ['deriveBits']);
# const derivedBits = await crypto.subtle.deriveBits({ name: 'PBKDF2', salt: encoder.encode(salt), iterations: 100000, hash: 'SHA-256' }, keyMaterial, 256);
# const hash = Array.from(new Uint8Array(derivedBits)).map(b => b.toString(16).padStart(2, '0')).join('');
# console.log('Salt:', salt);
# console.log('Hash:', hash);